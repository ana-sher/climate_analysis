services:
  app:
    build: .
    container_name: climate-analysis-api
    command: uvicorn "src.api:app" --host 0.0.0.0 --port 80 --reload
    ports:
      - "8080:80"
    volumes:
      - .:/app
      - climate-analysis-data:/app/data

volumes:
  climate-analysis-data: